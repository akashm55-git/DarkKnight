function showAlert(a, b) {
        let sum;
        const product = a * b;
        sum = a + b;
        let numbers = [1, 2, 3, 4, 5];
        let obj_name = {"key1":"ironman","key2":"thor"};
        alert("Element " + numbers[0] + " from array and object " + obj_name.key1)
        console.log("sum : " + sum);
        console.log("product :" + product);
        
}

function byTwo(a, b) {
        try {
        console.log(a/v)
        }
        catch {
        console.log("An error occurred:" + error.message);
        }
        finally {
                console.log("Execution Completed.")
        }
        }

